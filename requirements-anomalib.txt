# Зависимости для conda-окружения "anomalib" (см. configs/paths/local.yaml,
# python_envs.anomalib) — в нём запускается models/padim/.
# anomalib сам тянет за собой torch, pytorch-lightning и остальные зависимости.
anomalib>=2.0.0
