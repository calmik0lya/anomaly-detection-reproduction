# Зависимости для conda-окружения "anomalib" (см. configs/paths.yaml,
# python_envs.anomalib) — в нём запускается padim_run/ (configs/padim.yaml).
# anomalib сам тянет за собой torch, pytorch-lightning и остальные зависимости.
anomalib>=2.0.0
