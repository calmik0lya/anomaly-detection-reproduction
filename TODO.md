# TODO

Открытые пункты, что ещё можно улучшить:

- **PaDiM только для `bottle`.** `padim_run/run_padim.py` не принимает CLI-аргументов —
  категория, датасет и backbone зашиты внутри скрипта. Стоит научить его читать
  `category`/`mvtec_path` из аргументов (по образцу остальных моделей), чтобы
  `configs/padim.yaml` реально управлял запуском, а не был просто справкой.
- **CI.** Добавить GitHub Actions workflow, который на каждый push/PR ставит
  `requirements.txt` и гоняет `pytest` (тесты в `tests/` не требуют GPU/датасета,
  так что это дёшево). Заодно можно линтить YAML-конфиги (`yamllint`).
- **Пины версий.** `requirements-patchcore.txt`/`requirements-anomalib.txt` сейчас
  задают только нижние границы версий (`>=`). Для честной воспроизводимости стоит
  зафиксировать точные версии (например, через `pip freeze` из рабочих conda-окружений)
  хотя бы для torch/torchvision/anomalib.
- **Другие категории MVTec.** Все текущие результаты (`results.md`) — только для
  `bottle`. Было бы полезно прогнать все 5 моделей на нескольких категориях и
  агрегировать метрики (например, отдельным скриптом поверх `experiments/*/metrics.json`).
- **Fail-fast проверки путей.** `run.py` сейчас падает уже внутри модели, если
  `mvtec_path`/`dtd_images_path` из `configs/paths.yaml` не существуют. Стоит
  проверять их в начале `main()` и сразу давать понятную ошибку.
- **Сид для STFPM/DRAEM.** У PatchCore и SimpleNet в конфиге есть `seed`, у STFPM и
  DRAEM — нет (хотя их скрипты, возможно, воспроизводимы только частично). Стоит
  свести к единому виду, если/когда модели начнут это поддерживать.
- **Агрегация экспериментов.** `experiments/<model>__<category>__<timestamp>/` растёт
  с каждым запуском. Не помешал бы небольшой скрипт, который сводит все
  `metrics.json` из `experiments/` в одну таблицу для сравнения запусков.
